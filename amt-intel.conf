# WIP
# don't listen to INPUT/OUPUT, the machine will never see the packets
# https://mjg59.dreamwidth.org/48429.html

chain FORWARD {
  proto tcp dport (16992 16993) {
    LOG log-prefix "AMT Intel attack possibly detected";
    LOG;
    DROP;
  }
}
